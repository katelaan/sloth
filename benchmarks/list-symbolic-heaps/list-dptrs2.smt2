(declare-const a sl.list.loc)
(declare-const b sl.list.loc)
(declare-const c sl.list.loc)
(declare-const d Int)
(declare-const e Int)
(assert (sl.sepcon (sl.sepcon (sl.list.neq d e)
                              (> d 100))
                   (sl.sepcon (sl.list.dpointsto a b d)
                              (sl.list.dpointsto b c e))))
