(declare-const a sl.list.loc)
(declare-const b sl.list.loc)
(declare-const c sl.list.loc)
(declare-const d sl.list.loc)
(declare-const e sl.list.loc)
(declare-const f sl.list.loc)
(declare-const g sl.list.loc)
(declare-const h sl.list.loc)
(declare-const i sl.list.loc)
(declare-const j sl.list.loc)
(declare-const k sl.list.loc)
(declare-const l sl.list.loc)
(declare-const m sl.list.loc)
(declare-const n sl.list.loc)
(declare-const o sl.list.loc)
(declare-const p sl.list.loc)
(assert (sl.sepcon
         (sl.sepcon
          (sl.sepcon (sl.sepcon (sl.list.seg a b)
                                (sl.list.seg b c))
                     (sl.sepcon (sl.list.seg c d)
                                (sl.list.seg d e)))
          (sl.sepcon (sl.sepcon (sl.list.seg e f)
                                (sl.list.seg f g))
                     (sl.sepcon (sl.list.seg g h)
                                (sl.list.seg h i))))

         (sl.sepcon
          (sl.sepcon (sl.sepcon (sl.list.seg i j)
                                (sl.list.seg j k))
                     (sl.sepcon (sl.list.seg k l)
                                (sl.list.seg l m)))
          (sl.sepcon (sl.sepcon (sl.list.seg m n)
                                (sl.list.seg n o))
                     (sl.sepcon (sl.list.seg o p)
                                (sl.list.seg p a))))))
